<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../partials/head', {title: title}) %>
</head>
<body>
    <header class="content-header">
        <%- include('../partials/header', {isAuthenticated: isAuthenticated, user: user}) %>
    </header>

    <% if (isAuthenticated) { %>
        <h1>Create project</h1>
    <% } else { %>
        <h1>Welcome</h1>
    <% } %>

    <footer>
        <%- include('../partials/footer') %>
    </footer>

    <%- include('../partials/scripts') %>
</body>
</html>